import { useSelector } from 'react-redux'
import idProduct from '../redux/actions/productsActions'

function ProductDetails () {
  const id = useSelector()

  return (
    <>
    </>
  )
}

export default ProductDetails
